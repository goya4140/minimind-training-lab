# VLM 固定定性评估集

六张图片来自锁定版本 `jingyaogong/minimind-v@740d467` 的 `dataset/eval_images/`，
随上游 Apache-2.0 代码仓库用于复现评估。`prompts.json` 固定 6 个单图案例和 1 个复用其中
两图的顺序/指代案例。最终报告必须原样展示全部输出，并同时分析正确描述、遗漏与幻觉，
不能只挑选成功案例。
